
{% extends 'guest/base.html.twig' %}

{% block metatitle %}
	ElectroMénager | Détail de {{ product.title }}
{% endblock metatitle %}

{% block main %}
		<article>

		<h1>{{ product.title }}</h1>
		<p>{{ product.description }}</p>
		<p>Prix : {{ product.price }}</p>
		
		<p>Créé le {{ product.createdAt | date('d-m-Y') }}

	</article>
{% endblock main %}
	